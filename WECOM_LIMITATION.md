# 企业微信集成限制说明

**问题**: 企业微信不支持 API 轮询获取消息

---

## 📊 企业微信 vs 飞书/钉钉

| 平台 | WebSocket | HTTP 回调 | API 拉取消息 |
|------|-----------|----------|-------------|
| **飞书** | ✅ 支持 | ❌ 不需要 | ❌ 不支持 |
| **钉钉** | ✅ 支持 | ❌ 不需要 | ❌ 不支持 |
| **企业微信** | ❌ 不支持 | ✅ 必须 | ❌ 不支持 |

---

## 🎯 企业微信的实际情况

### 官方架构

```
企业微信服务器 → HTTP POST → 你的公网服务器
                      ↓
                  验证签名
                      ↓
                  解析消息
                      ↓
                  调用 Agent
                      ↓
                  API 回复
```

**特点**:
- ✅ **只能被动接收**（回调）
- ❌ **不能主动拉取**（无 API）
- ❌ **没有 WebSocket**

---

## 💡 解决方案

### 方案 1: 内网穿透（推荐）

**工具**: ngrok / frp / 花生壳

**架构**:
```
企业微信 → ngrok 公网地址 → 内网 localevobot
```

**步骤**:

1. **安装 ngrok**
   ```bash
   pip install pyngrok
   ```

2. **修改 channel.py**
   ```python
   from pyngrok import ngrok
   
   # 启动内网穿透
   tunnel = ngrok.connect(8888)
   print(f"回调地址：{tunnel.public_url}/wecom/callback")
   ```

3. **配置企业微信**
   - 登录企业微信管理后台
   - 应用管理 → 自建应用
   - 设置回调 URL: `https://xxx.ngrok.io/wecom/callback`

**成本**:
- ngrok 免费版：随机域名，每天限制
- ngrok 付费版：$8/月，固定域名
- frp：免费，需要自己的服务器

---

### 方案 2: 云服务器（生产推荐）

**架构**:
```
企业微信 → 云服务器回调 → 内网 localevobot
```

**部署**:
1. 购买云服务器（阿里云/腾讯云，约 50 元/月）
2. 部署轻量回调服务
3. 通过 WebSocket/Redis 转发到内网

**优点**:
- ✅ 稳定可靠
- ✅ 生产环境
- ✅ 完全控制

**缺点**:
- ❌ 需要成本
- ❌ 需要运维

---

### 方案 3: 放弃企业微信（最简单）

**原因**:
- 企业微信架构确实需要公网
- 与飞书/钉钉架构不一致
- 增加维护成本

**替代**:
- 使用飞书（推荐）
- 使用钉钉（推荐）
- 使用企业微信仅作为通知渠道（单向）

---

## 🎯 我的建议

### 对于 localevobot

**推荐方案**:

1. **飞书 + 钉钉** → WebSocket 长连接（已完成）
   - ✅ 无需公网
   - ✅ 实时通信
   - ✅ 架构一致

2. **企业微信** → 暂时搁置 或 仅用于通知
   - ❌ 需要公网
   - ⚠️ 架构不一致
   - ✅ 可以作为通知渠道（单向发送）

---

## 📋 当前实现

### 已删除回调模式
```python
# channels/wecom/channel.py
# 回调模式已移除，默认使用 API 轮询（无需公网）
```

### 问题
```python
def _get_messages(self) -> list:
    """获取消息列表"""
    # TODO: 实现消息获取
    # 企业微信官方不支持主动拉取消息
    return []  # ← 返回空列表
```

**结论**: 当前实现无法接收消息，需要回调模式或内网穿透。

---

## 💡 最终建议

### 选项 A: 使用内网穿透（如果想支持企业微信）

**工作量**: 2-4 小时

**步骤**:
1. 安装 pyngrok
2. 恢复回调模式代码
3. 启动内网穿透
4. 配置企业微信回调 URL

---

### 选项 B: 放弃企业微信（推荐）

**原因**:
- 飞书/钉钉 已经足够
- 企业微信需要公网，架构不一致
- 增加维护成本

**操作**:
```bash
# 删除企业微信 Channel
Remove-Item -Recurse "D:\localevobot\langchain\channels\wecom"
```

---

### 选项 C: 仅保留发送功能

**用途**: 通知推送（单向）

**实现**:
```python
# 保留 send_message() 方法
# 删除消息接收功能
```

---

## 📝 总结

| 平台 | 连接方式 | 需要公网 | 状态 |
|------|---------|---------|------|
| **飞书** | WebSocket | ❌ | ✅ 完成 |
| **钉钉** | WebSocket | ❌ | ✅ 完成 |
| **企业微信** | HTTP 回调 | ✅ | ⚠️ 需要内网穿透 |

**建议**: 
- ✅ 使用飞书/钉钉（无需公网）
- ⚠️ 企业微信需要内网穿透或云服务器
- ❌ 或者放弃企业微信支持
