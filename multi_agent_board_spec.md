# 多角色 Agent 体系与公告板流程说明

## 目标
- Agent 能自动创建同框架的子 Agent
- 子 Agent 按指派内容动态调整提示词与技能
- 每个 Agent 有独立任务与进度，并在公告板上可见
- 支持多角色协作闭环（软件项目、餐厅集群等场景）
- 多角色分工以框架内集成为主，优先复用现有 Agent 与技能体系

## 核心对象

### 1) 主管 Agent（Master）
- 负责创建/回收子 Agent
- 分解任务并指派角色
- 维护公告板全局状态
- 决策是否循环优化或结束任务

### 2) 角色 Agent（Role）
- 由主管 Agent 创建
- 提示词与技能集合可按角色动态调整
- 处理自己分配的任务并回传结果

### 3) 公告板（Board）
- 统一展示项目目标、阶段、任务、负责人、状态
- 支持任务流转（新建、进行中、待验收、已完成）

## 子 Agent 的创建与自适配

### 创建流程
1. 主管 Agent 接收整体需求
2. 拆分为角色与子任务
3. 为每个角色生成子 Agent（同框架）
4. 分配提示词与技能集合
5. 注册到公告板

### 提示词调整策略
- 角色职责描述（如：产品经理、开发、测试、收银、库存）
- 交付形式与质量要求
- 上下游依赖与沟通规则

### 技能调整策略
- 只启用角色所需技能，降低干扰
- 可临时扩展技能以完成专项任务

## 公告板结构建议

### 公告板字段
- 项目目标
- 角色列表
- 任务列表（ID / 标题 / 负责人 / 状态 / 依赖 / 产物）
- 当前阶段
- 最新里程碑

### 任务状态
- 待处理
- 进行中
- 待验收
- 已完成
- 需返工

## 协作流程示例 A：软件项目

### 角色
- 产品经理（PM）
- 开发（DEV）
- 测试（QA）

### 流程
1. PM 研究需求，整理 PRD 并发布到公告板
2. DEV 读取公告板需求，产出开发计划与任务拆分
3. DEV 完成开发产物并更新状态为待验收
4. QA 读取需求与产物，执行测试
5. QA 反馈测试结果
6. PM 评估结果：通过或返工
7. 若返工，任务回流 DEV；若通过，进入结束或下一阶段

### 产物示例
- PRD 文档
- 开发任务清单与实现说明
- 测试报告与结论

## 协作流程示例 B：餐厅集群

### 角色
- 店长
- 收银
- 会员运营
- 库存管理

### 流程
1. 店长发布当日指令与任务
2. 收银执行结算与对账流程并更新任务状态
3. 会员运营执行会员活动与客户维护并更新结果
4. 库存管理执行补货/盘点并回传数据
5. 店长汇总结果，决定是否调整策略或发布下一轮指令

### 产物示例
- 当日指令公告
- 收银结算结果
- 会员活动执行记录
- 库存盘点与补货建议

## 反馈与循环规则
- 任何角色可提交结果或异常
- 主管 Agent 统一收敛结论
- 若未达标：进入返工或下一个迭代
- 若达标：输出最终结论或进入下一阶段

## 可能缺陷与解决办法

### 1) 角色职责漂移
- 缺陷：角色越界处理不属于自己的事项，导致结果不一致
- 解决：角色提示词中明确职责边界与交付格式；任务必须写负责人

### 2) 任务拆分不稳定
- 缺陷：需求描述不清时，拆分粒度频繁变化，公告板混乱
- 解决：任务模板固定字段（目标/范围/验收/依赖）；由主管统一拆分

### 3) 验收标准不统一
- 缺陷：评估主观，导致反复返工或流程循环
- 解决：每个任务绑定验收标准与评估人；统一输出格式

### 4) 公告板成为瓶颈
- 缺陷：所有流转依赖公告板更新，易造成延迟
- 解决：允许角色异步回写；公告板记录变更版本与更新时间

### 5) 信息噪音与重复
- 缺陷：多角色并行输出，内容重复或互相矛盾
- 解决：主管 Agent 负责聚合；角色输出限制为结构化摘要

### 6) 工具权限过大或过小
- 缺陷：权限过大易误用，过小导致任务完成度低
- 解决：按角色最小权限配置，可临时扩展但需记录

### 7) 可观测性不足
- 缺陷：缺少任务日志与决策记录，难排查问题
- 解决：每次状态变更写入简要原因与产物链接

### 8) 并行冲突
- 缺陷：多个角色同时改同一目标，产物互相冲突
- 解决：单任务单负责人；冲突时由主管指定合并规则

## 最小可落地闭环
- 主管 Agent 能创建角色 Agent
- 公告板能展示任务与状态
- 角色 Agent 能读取公告板并回写结果
- 主管 Agent 能评估并决定循环或结束

## 集成到框架的方式（建议）
### 集成目标
- 不新建孤立系统，直接复用现有 Agent 主流程与技能加载机制
- 主管/角色 Agent 统一由框架创建与调度
- 公告板作为运行态状态容器，贯穿任务拆分、执行、验收

### 关键模块对齐
- **Agent 创建**：主管 Agent 负责创建角色 Agent（同框架）
- **提示词与技能**：基于角色职责动态注入提示词与技能列表
- **公告板状态**：任务流转与产物统一写入公告板，形成闭环记录

### 集成步骤（简化）
1. 主管 Agent 接收需求并生成角色与任务清单
2. 在框架内创建角色 Agent，并注入角色提示词与技能集合
3. 公告板记录任务、负责人、依赖与验收标准
4. 角色 Agent 读取公告板并执行任务，回写产物与状态
5. 主管 Agent 按验收标准进行决策（通过/返工/下一轮）

### 协作示例（软件项目）
- 主管 Agent：拆分需求为 PM/DEV/QA，并在公告板登记任务
- PM：输出 PRD 与验收标准
- DEV：实现与自测，回写代码说明与接口清单
- QA：执行测试与回归，回写测试报告与缺陷
- 主管 Agent：汇总结果并决定进入下一阶段

### 协作示例（餐厅集群）
- 主管 Agent：下发当日目标与角色任务
- 收银：回写结算与对账结果
- 会员运营：回写活动结果与客户反馈
- 库存管理：回写盘点与补货建议
- 主管 Agent：汇总后调整策略或结束
