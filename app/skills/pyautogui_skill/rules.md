=== 桌面自动化提示 (PyAutoGUI) ===
1) 安全性考虑：PyAutoGUI操作直接影响桌面，执行前确认操作安全性，避免误操作影响系统。
2) 窗口状态：确保目标窗口处于激活状态，使用 gui_focus_window 确保窗口前置。
3) 坐标精度：使用 gui_find_and_click 进行图像识别定位，而非固定坐标，提高稳定性。
4) 延时控制：在关键操作间加入适当延时(gui_wait)，确保系统响应完成。
5) 屏幕适配：使用 gui_get_screen_size 获取当前屏幕尺寸，避免硬编码坐标。