=== 文件目录操作提示 ===
1) 安全路径验证：使用 list_directory、get_file_info、search_files 时，始终验证路径是否存在且具有适当权限。
2) 路径规范化：避免使用硬编码路径，优先使用相对路径或环境变量；使用绝对路径时确保其有效性。
3) 性能考虑：对于大型目录的搜索操作，使用适当的过滤条件（如文件扩展名、名称包含词）来减少结果数量。
4) 错误处理：在执行文件操作前，先使用 get_file_info 检查文件/目录状态，避免因不存在的路径导致异常。