=== 数据库操作安全规范 ===
1) 连接管理：使用 configure_sqlserver 配置连接池参数，避免连接泄露；操作完成后确保连接正确释放。
2) SQL安全：使用参数化查询防止SQL注入；敏感操作(DDL/DML)前先进行语法检查(test_sql_syntax)。
3) 事务处理：批量操作时使用 execute_batch_sql 确保原子性；重要数据变更前先备份。
4) 性能监控：复杂查询前使用 analyze_sql_performance 检查执行计划；避免N+1查询问题。
5) 敏感信息：数据库凭证使用环境变量或加密存储，避免硬编码在代码中。